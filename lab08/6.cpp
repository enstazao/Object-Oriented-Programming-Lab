#include <iostream>  //Including the header file input output stream
using namespace std;  //USing the std to use cout with std::cout

int main()   //Main Function from where an integer is returned and execution starts from here
{
    int *ptr = NULL;  //Intializing the Pointer with the NULL
    cout << "The Value of the Pointer after initialize: " << ptr << endl;  //Printing the Value of the ptr
    return 0;   //Returning the Integer Value
}